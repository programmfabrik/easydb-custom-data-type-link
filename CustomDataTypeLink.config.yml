plugin:
  name: custom-data-type-link
  version: 1.0
  server:
    api-version:
      require: 1
  webfrontend:
    url: custom-data-type-link.js
    l10n: l10n/
custom_types:
  link:
    elasticsearch:
      mapping:
        type: l10ntext
      value:
        origin: json
        key: text
    config:
      schema:
        - name: add_timestamp
          parameters:
            value:
              type: bool
      mask:
        - name: editor_style
          parameters:
            value:
              type: select
              options: ["inline", "popover"]
